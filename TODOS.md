# TODOS

- [ ] **P1 · Card visual decision** — choose figure well or art for every card and delete the other. The front currently mixes both. Effort S. Added by /plan-ceo-review 2026-09-17.
- [ ] **P3 · Feeds as beehiiv input** — RSS and JSON Feed generated by Astro from Sanity content (Sanity stays the source; the feed is a bridge to the newsletter). Effort S. Depends on Sanity cutover.
- [ ] **Parked · Refresh rule for data blocks** — new piece vs flag-for-edit. Not before design and structure are locked. No refresh script until decided.
- [ ] **P1 · Publish webhook (blocked on operator)** — Sanity → rebuild on publish. Needs either a Vercel deploy hook (preferred; run `npx vercel login` then `npx vercel link`) or a GitHub fine-grained token for `repository_dispatch`. Filter the Sanity webhook to `_type == "piece" && status in ["published","corrected"]`. Until then, a rebuild is a push to main or a manual Actions run.
- [ ] **P2 · Vercel hosting, then retire Pages + Render** — same login as above. Build command is `npm run build`, output `dist`, env `SITE_URL` set to the production origin and `SITE_BASE` unset.
