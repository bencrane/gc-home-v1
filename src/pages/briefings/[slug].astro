---
import Base from "@/layouts/Base.astro"
import PiecePage from "@/views/PiecePage"
import { bySection } from "@/content/pieces"
export function getStaticPaths() {
  return bySection("briefings").map((p) => ({ params: { slug: p.slug }, props: { title: p.title, dek: p.dek } }))
}
const { slug } = Astro.params
const { title, dek } = Astro.props
---
<Base title={title} description={dek}>
  <PiecePage slug={slug!} />
</Base>
